"use client";
import { createTheme } from "@mui/material";

export default createTheme({
  palette: {
    primary: {
      // main: "#1976d2",
      main: "#202A44",
      // light: "",
      dark: "#324268"
    }
  },
  typography:  {
    fontFamily: "Calibri"
  }
});